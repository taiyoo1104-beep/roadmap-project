FROM node:24-slim

WORKDIR /app

# 先に package.json などをコピーしてインストール（キャッシュを効かせるため）
COPY package.json package-lock.json ./
RUN npm install

# 残りのファイルをコピー
COPY . .

# Vite の開発サーバーを起動（全ホストからの接続を許可）
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]